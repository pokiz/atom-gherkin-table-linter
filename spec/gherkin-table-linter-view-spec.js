'use babel';

import GherkinTableLinterView from '../lib/gherkin-table-linter-view';

describe('GherkinTableLinterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
